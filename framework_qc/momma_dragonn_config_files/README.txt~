The yaml files that have "master" pre-prended to them are designed to work with the Master branch of the momma_dragonn repository.
The yaml files that have "anna" pre-prended will work with anna_dev
The remaining files are compatible with both branches
